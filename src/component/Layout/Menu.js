import styled from "styled-components";

const Menu = () => {
    return <MenuWrapper>sc</MenuWrapper>;
};

export default Menu;

const MenuWrapper = styled.div`
    width: 100%;
    height: 100vh;
    position: fixed;
    top: 0;
    left: 0;
`;
